// we are exporting in default mode, so it's reachable

export const Role = {
    USER: 'USER',
    ADMIN: 'ADMIN'
}

// remember? the "Role" in Spring Boot is 
// technically an enum...
// there is no "Enum" description in React.js

// with "const" we can describe 
// final constant variables (like Enum fields)