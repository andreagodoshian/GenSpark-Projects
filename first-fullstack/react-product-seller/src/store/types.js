// redux is a store management system...
// that's why the folder is called "store" :)

// to access the different actions, we need types!!!

export const SET_CURRENT_USER = 'SET_CURRENT_USER';
export const CLEAR_CURRENT_USER = 'CLEAR_CURRENT_USER';

// types -> actions -> reducers -> describe/index -> inform app

// make sure you inform the app to use the store!!
// go to the "main" index.js